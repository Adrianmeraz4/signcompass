<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link rel="stylesheet" href="/styles/style.css">
	<title>Sign In and Sign Up Buttons</title>
</head>

<body>
	<div class="bg-primary">
		<div
			class="container max-w-6xl flex justify-center lg:justify-between items-center flex-col lg:flex-row mx-auto px-20 min-h-screen">
			<h1 class="site-title text-white font-bold">Sign Compass</h1>
			<div class="background-container !h-[unset]">
				<div class="signin-form" id="signin-form">
					<form method="post" class="px-8">
						<p class="mb-2 text-black">In order to sign up or sign in input a valid email & password</p>

						<p class="mb-1 hidden text-[#FF0000]" id="emailError">Password or email is incorrect</p>
						<label class="text-black hidden" for="username">Email:<br></label>
						<input class="text-black outline-none focus:border-primary w-full" type="text" name="username"
							placeholder="Email@mail.com" required pattern="[^@\s]+@[^@\s]+"
							title="Must be a valid email">

						<label class="text-black hidden" for="password">Password:<br></label>
						<input class="text-black outline-none focus:border-primary w-full" type="password"
							name="password" placeholder="Password" required
							pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[`!@#$%^&*]).{10,}"
							title="Password must contain an uppercase/lowercase, special character and minimum of 10 characters">
						<br>

						<div class="flex justify-between gap-x-4 mt-2">
							<a class="back-button !bg-[#ebf5fb] !border-primary border-2 !text-primary flex !pl-0"
								href="/" id="signin-back-button">
								<svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"
									fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
									stroke-linejoin="round" class="lucide lucide-chevron-left">
									<path d="m15 18-6-6 6-6" />
								</svg>
								Back</a>
							<button class="back-button" type="submit">Submit</button>
						</div>
						<!-- Added Forgot Password Link -->
                        			<div class="mt-3 text-center">
                            				<a href="#" id="forgot-password" class="text-primary">Forgot Password?</a>
                        			</div>
						<script>
                            				const urlParams = new URLSearchParams(window.location.search);
                            				const error = urlParams.get('error');

                            				if (error) {
                                				document.querySelector('#emailError').classList.remove('hidden');
                               					 document.querySelector('input[name="username"]').value = urlParams.get('email');
                            					}

                           			 // JavaScript for Forgot Password functionality
                            				document.getElementById('forgot-password').addEventListener('click', function(event) {
                                			event.preventDefault();
                                			var email = prompt('Please enter your email to reset your password:');
                                			if(email) {
                                    				// You would normally handle the email with a backend service here
                                    				console.log('Reset password email sent to: ' + email);
                                }
                            });
                        </script>
					</form>
				</div>
			</div>
		</div>
	</div>
</body>

</html>
